{
  "name": "ref-tool-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for PDF generation API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --platform=node",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^4.45.4",
    "esbuild": "^0.25.12"
  }
}
