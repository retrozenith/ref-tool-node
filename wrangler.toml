# Unified Cloudflare Workers configuration for this repository
# This file was generated by the cleanup script and merges the previous
# worker/wrangler.toml and wrangler.jsonc files. Replace placeholders
# (YOUR_ACCOUNT_ID, YOUR_ZONE_ID) with real values or set them as
# environment variables before deploying.

name = "gra"
main = "worker/dist/worker.js"
type = "javascript"
compatibility_date = "2025-11-11"
[compatibility_flags = ["nodejs_compat"]

# Workers dev should be a boolean. Set to true to enable the workers.dev
# testing subdomain. The previous file mistakenly defined a `[workers_dev]`
# table which caused a type error in Wrangler.
workers_dev = true

# Note: custom build removed to avoid running build from the wrong working
# directory during `wrangler deploy`. The Worker uses the prebuilt bundle at
# `main = "worker/dist/worker.js"`. Build the bundle before deploying (CI
# should run `npm --prefix ./worker run build` once and produce `worker/dist`).

[site]
# Static assets (publicly served) for the worker
bucket = "./worker/public"

[vars]
ENVIRONMENT = "development"

[env.production]
vars = { ENVIRONMENT = "production" }